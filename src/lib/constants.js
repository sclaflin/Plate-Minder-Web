export const NOOP = () => {};
export const DEFAULT_GROUP_INFO = 'Default group information text.';
export const DEFAULT_GROUP_EMPTY = 'To get started, select an item type below and click "Add".';
export const SOURCE_GROUP_INFO = 'Sources are the video streams that Plate-Minder gathers images from. Without a working source, no plate information can be captured.';
export const FILTER_GROUP_INFO = 'Filters modify the images gathered from sources and process sequentially from top to bottom. While optional, they can increase performance and help remove false positives.';
export const RECORDER_GROUP_INFO = 'Recorders capture detected plate information and store/report it in various ways. Think of them as how information flows out of Plate-Minder.';
export const DEFAULT_ITEM_INFO = 'Default item information text.';
export const RTSP_SOURCE_ITEM_INFO = 'RTSP is typically used with IP cameras.';
export const FILE_SOURCE_ITEM_INFO = 'Using a file as a source for video is useful on occasions when you need to process a pre-recorded video manually.';
export const MASK_FILTER_ITEM_INFO = 'Masks allow you to block out parts of the video that shouldn\'t be processed. For example, it\'s best to mask out a timestamp to avoid false positives of motion detection.';
export const MOTION_FILTER_ITEM_INFO = 'Motion detection focuses on the largest area of movement. When there\'s no motion, there\'s no attempts at detecting license plates.';
export const SQLITE_RECORDER_ITEM_INFO = 'Detected license plate information is stored in a SQLite database. This recorder requires no configuration.';
export const MQTT_RECORDER_ITEM_INFO = 'Detected license plate information is sent to an MQTT server. This is useful for integrating with other MQTT compatible services. Home Assistant autodiscovery support is built in.';
export const FILE_RECORDER_ITEM_INFO = 'Detected license plate images are stored locally to disk. The folders and file names generated are customizable via tokens.';
export const DEFAULT_SOURCE_NAME = 'Source Name';
export const DEFAULT_SOURCE_CAPTURE_INTERVAL = 1;
export const DEFAULT_SOURCE_STATUS_INTERVAL = 3000;
export const DEFAULT_SOURCE_PRE_INPUT_ARGS = [];
export const DEFAULT_SOURCE_PRE_OUTPUT_ARGS = [];
export const DEFAULT_FILE_SOURCE_FILE = './path/to/your/file.mp4';
export const DEFAULT_RTSP_SOURCE_URL = 'rtsp://your-camera-url';
export const DEFAULT_FILTER_DEBUG = false;
export const DEFAULT_FILTER_MASK = ['0,0,100,0,100,100,0,100'];
export const DEFAULT_RECORDER_PATTERN = '/images/{{DATE}}/{{SOURCE}}/{{TIME}}_{{PLATE}}.jpeg';
export const DEFAULT_RECORDER_RETAIN_DAYS = 30;
export const DEFAULT_RECORDER_URL = 'mqtt://your-mqtt-server';
export const DEFAULT_RECORDER_BASE_TOPIC = 'plate-minder';
export const DEFAULT_RECORDER_MQTT_OPTIONS = { username: 'your-username', password: 'your-password' };
export const DEFAULT_RECORDER_HASS_DISCOVERY = { enable: false, discoveryPrefix: 'homeassistant' };
